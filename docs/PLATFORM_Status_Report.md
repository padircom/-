# گزارش وضعیت کلی پلتفرم Arena — همهٔ ماژول‌ها

تاریخ: ۱۸ شهریور ۱۴۰۵ (۲۰۲۶-۰۹-۰۹) · شاخه: `arena/01a07ee6-repo` · آخرین کامیت: `8ae4592`

همهٔ اعداد این گزارش با اجرای واقعی روی مخزن استخراج شده‌اند، نه از حافظه.

---

## ۱. تصویر کلی

| سنجه | مقدار |
|---|---|
| جدول‌های اسکیما | **۹۵** |
| مهاجرت‌ها | **۱۷** (`0001` تا `0017`) |
| مجوزهای RBAC | **۱۰۱** |
| نقش‌ها | **۲۱** |
| اندپوینت REST | **۱۹۳** |
| فایل آزمون | **۴۲** |
| آزمون‌های سبز | **۱۶۸۰ / ۱۶۸۰** |
| خطای TypeScript | ۵۷ (baseline ثابت، همگی از پیش موجود) |
| build تولیدی | سبز · `dist/index.html` ۱٬۴۵۵ kB |
| موتور خالص (`build:*`) | **۱۵** |
| پنل UI | **۲۹** |
| دامنهٔ سایدبار | **۱۴** |
| سند تحویلی | **۸۷** فایل در `docs/` |

---

## ۲. وضعیت ماژول‌ها

### ۲٫۱ ماژول‌های بسته‌شده

| # | ماژول | دامنه | جدول | وضعیت |
|---|---|---|---|---|
| ۱ | مدیریت اطلاعات و مستندات (PIM/DMS) | `d1` | ۲ | ✅ بسته — ۹ تحویلی + گزارش کیفی |
| ۲ | برنامه‌ریزی و اجرای عملیات (PEX) | `d2` | ۶ | ✅ بسته — ۱۳ تحویلی + ۲ گزارش کیفی |
| ۳ | پایش و کنترل عملکرد (PMA) | `d3` | ۲ | ✅ بسته — ۲ تحویلی + گزارش کیفی |
| ۴ | ریسک، تغییرات و ادعاها (RCC) | `d4` | ۳ | ✅ بسته — ۱۴ تحویلی + گزارش کیفی |
| ۵ | هزینه، تأمین و لجستیک (FIN) | `d5` | ۴ | ✅ بسته |
| ۶ | حاکمیت و PMBOK (GOV) | `d6` | — | ✅ بسته — ۱۰ تحویلی + گزارش کیفی |
| ۷ | سامانه و پیکربندی پایه | `d7` | ۵ | ✅ بسته — همیشه آخر سایدبار |
| ۸ | کیفیت و بازرسی (QMS) | `d8` | ۲ | ✅ بسته |
| ۹ | منابع انسانی (HRM) | `d9`/`d10` | ۱۰ + ۲ | 🟡 D1–D3 بسته · **D4–D9، D11–D13 متوقف** |
| ۱۰ | دانش پروژه (CKM) | `d11` | ۳ | ✅ بسته |
| ۱۱ | مهندسی و طراحی (ENG) | `d12` | ۹ | ✅ بسته — ۳۱ اندپوینت |
| ۱۲ | ماشین‌آلات و تجهیزات (EQP) | — | — | ✅ بسته — بدون آیتم سایدبار طبق دستور |
| ۱۳ | راه‌اندازی و تحویل (COM) | `d15` | ۸ | 🟡 D1–D4 بسته · **D5–D12 باز** |
| ۱۴ | پیمان و صورت‌وضعیت (CNT) | `d14` | **۲۳** | 🟡 ۷ از ۱۴ تحویلی · **D7–D13 متوقف** |
| ۱۵ | ایمنی، بهداشت و محیط‌زیست (HSE) | `d16` | **۱۲** | 🟢 **در حال کار** — D1–D4 بسته |

**زیرساخت‌های افقی (همه بسته):** گزارش‌ساز (۶ تحویلی) · RBAC (۸) · ماندگاری SQL (۵) · یکپارچه‌سازی (۷).

---

### ۲٫۲ ماژول فعال: HSE

آخرین کار انجام‌شده. چهار تحویلی از چهارده:

| تحویلی | وضعیت | خروجی |
|---|---|---|
| D1 معماری | ✅ | `HSE_Architecture.md` — ۱۲ ADR، ۱۴ شکاف GH-01..14 |
| D2 مدل داده | ✅ | `HSE_DataModel.md` — ۲۹ جدول طرح‌شده، ERD کامل |
| D3 ارزیابی ریسک شغلی | ✅ | ۴ جدول · مهاجرت `0016` · ۷۸ آزمون |
| D4 سامانهٔ پروانهٔ کار | ✅ | ۴ جدول + ۶ ستون · مهاجرت `0017` · ۱۱۵ آزمون |
| D5 حوادث و شبه‌حوادث | ⛔ | — |
| D6 بازرسی، تخلفات، SWO | ⛔ | — |
| D7 آموزش و محیط‌زیست | ⛔ | — |
| D8–D14 | ⛔ | — |

**شکاف‌های بسته‌شده:** GH-05 (پیوند پروانه↔ارزیابی ریسک) · GH-06 (گازسنجی عددی) · GH-07 (قفل و برچسب).
**شکاف بحرانی باقی:** GH-04 (دستور توقف کار / SWO) · GH-01 (PHI ایمنی هنوز عدد ثابت ۹۰ در `projectControls.ts:48`).

---

## ۳. توزیع جدول‌ها

```
d14 پیمان            ███████████████████████  ۲۳
d16 ایمنی            ████████████             ۱۲
d9  منابع انسانی      ██████████               ۱۰
d12 مهندسی           █████████                 ۹
d15 راه‌اندازی        ████████                  ۸
d2  برنامه‌ریزی        ██████                    ۶
d7  سامانه           █████                     ۵
d5  مالی · core      ████                      ۴ + ۴
d4 · d11             ███                       ۳ هرکدام
d1 · d3 · d8 · d10   ██                        ۲ هرکدام
```

---

## ۴. توزیع اندپوینت‌ها

| پیشوند | تعداد | | پیشوند | تعداد |
|---|---|---|---|---|
| `/api/eng` | ۳۱ | | `/api/qms` | ۱۱ |
| `/api/hse` | **۲۴** | | `/api/data` | ۹ |
| `/api/com` | ۲۲ | | `/api/pex` · `/api/gov` | ۸ هرکدام |
| `/api/cnt` | ۲۱ | | `/api/integration` | ۵ |
| `/api/projects` | ۱۵ | | سایر (۱۶ پیشوند) | ۲۹ |
| `/api/eqp` | ۱۴ | | **جمع** | **۱۹۳** |

---

## ۵. 🔴 شکاف اصلی پلتفرم: رابط کاربری

این مهم‌ترین یافتهٔ گزارش است و باید صریح گفته شود.

**۱۲ دامنه از ۱۴ پنل اختصاصی مونت‌شده دارند:**
`d1` `d2` `d3` `d4` `d5` `d6` `d7` `d8` `d9` `d10` `d11` `d12`

**۲ دامنه پنل ندارند:**

| دامنه | ماژول | وضعیت |
|---|---|---|
| `d15` | راه‌اندازی و تحویل | ⛔ `CommissioningWorkspace.tsx` ساخته نشده |
| `d16` | ایمنی و بهداشت | ⛔ `HseWorkspace.tsx` ساخته نشده |

**اثر عملی:** کاربر روی «مدیریت ایمنی، بهداشت و محیط‌زیست» یا «راه‌اندازی و تحویل» در سایدبار کلیک می‌کند و **صفحهٔ عمومی ماژول** را می‌بیند، نه پنل کاری. تمام کار D3 و D4 ماژول HSE — ارزیابی ریسک، پروانهٔ کار، گازسنجی، LOTO، امضای سه‌سطحی — **فقط از REST قابل استفاده است**.

این در گزارش کیفی COM با کد **G-10** ثبت شده (برآورد ۵۶ نفر-ساعت) و معادلش برای HSE هنوز برآورد نشده.

---

## ۶. بدهی‌های فنی ثبت‌شده

| کد | شرح | دامنه | برآورد |
|---|---|---|---|
| **G-10** | `CommissioningWorkspace.tsx` ساخته نشده | COM | ۵۶ ساعت |
| **G-01** | قفل دروازهٔ گواهی‌ها (MC/RFSU/PAC/FAC) | COM | ۵۶ ساعت |
| **G-03** | رویداد مالی COM → FIN | COM | ۴۰ ساعت |
| **G-04** | آزمون عملکرد ۷۲ ساعته (PGTR) | COM | ۶۴ ساعت |
| **TD-HSE-01** | ۱۶۷ گزارهٔ `CREATE INDEX` غیریکتا بدون گارد ایدمپوتنت | سراسری (۱۷ مهاجرت) | ۸ ساعت |
| **GH-01** | PHI ایمنی عدد ثابت ۹۰ است (`projectControls.ts:48`) | HSE | برآورد نشده |
| **GH-04** | دستور توقف کار (SWO) پیاده نشده | HSE | برآورد نشده |
| — | پنل UI ماژول HSE | HSE | برآورد نشده |

**نکتهٔ TD-HSE-01:** در خودارزیابی D4 کشف شد که مسئلهٔ مرتبطی — ایندکس **یکتا** روی ستون nullable — باگی بود که مهاجرت را روی SQL Server واقعی می‌شکست. آن یکی در ریشه (`indexDdl`) اصلاح شد و سه ایندکس در سه ماژول مختلف را درست کرد. ولی گارد ایدمپوتنت برای ایندکس‌های غیریکتا هنوز باز است.

---

## ۷. کار انجام‌شده در این نشست

۶ کامیت:

| کامیت | شرح |
|---|---|
| `e41d3ad` | افزودن دامنهٔ `d16` به سایدبار (با تأیید صریح کاربر) |
| `e6a8007` | HSE D3 — ارزیابی ریسک شغلی و سلسله‌مراتب کنترل خطر |
| `0de1b15` | سند تحویلی D3 |
| `8d1643e` | رفع N+1 در فهرست ارزیابی ریسک + ۱۰ لوپ D3 |
| `f8a0dc2` | HSE D4 — سامانهٔ پروانهٔ کار |
| `8ae4592` | چهار یافتهٔ خودارزیابی D4 + ۱۰ لوپ |

**تغییر سنجه‌ها در این نشست:**

| سنجه | ابتدا | اکنون | Δ |
|---|---|---|---|
| جدول | ۸۷ | ۹۵ | +۸ |
| مهاجرت | ۱۵ | ۱۷ | +۲ |
| مجوز | ۸۸ | ۱۰۱ | +۱۳ |
| نقش | ۲۰ | ۲۱ | +۱ |
| آزمون | ۱۴۸۷ | ۱۶۸۰ | +۱۹۳ |
| اندپوینت HSE | ۰ | ۲۴ | +۲۴ |

**۵ یافتهٔ خودارزیابی که اصلاح شد** (نه صرفاً گزارش):
۱. N+1 در فهرست JSA — ۶۰۰۱ کوئری به ۴
۲. 🔴 ایندکس یکتای nullable — باگ شکنندهٔ مهاجرت روی SQL Server، در ریشه اصلاح شد
۳. 🔴 مقدار منفی گازسنج معیوب «ایمن» گزارش می‌شد
۴. قاعدهٔ ازسرگیری فقط در REST بود، به موتور منتقل شد
۵. `rfsuSafetyClearance` هیچ اندپوینتی نداشت — پل COM↔HSE وصل شد

---

## ۸. وضعیت اجرا

| سرویس | پورت | وضعیت |
|---|---|---|
| API server | ۴۰۰۰ | 🟢 در حال اجرا |
| Website (Vite) | ۵۱۷۳ | 🟢 در حال اجرا |

⚠️ `src/services/sqlServer.ts:30` پیش‌فرض `http://localhost:4000/api` دارد که **از مرورگر کاربر قابل دسترس نیست**؛ UI عمدتاً حالت offline/mock کار می‌کند.

---

## ۹. اولویت پیشنهادی

**کوتاه‌مدت (تکمیل HSE):**
۱. **D5 حوادث** — `SafetyIncident.PermitId` را می‌بندد و مصرف‌کنندهٔ مستقیم D4 است
۲. **D6 بازرسی و SWO** — GH-04 شکاف بحرانی است
۳. **D10 KPI/EWS** — GH-01 را می‌بندد (PHI از عدد ثابت ۹۰ به محاسبهٔ واقعی)

**میان‌مدت (بدهی انباشته):**
۴. **پنل‌های UI برای `d15` و `d16`** — بزرگ‌ترین شکاف پلتفرم؛ دو ماژول کامل بدون رابط کاربری
۵. **MOD-14 / CNT** — ۷ تحویلی باقی (D7–D13)
۶. **HRM** — D4–D9، D11–D13 (خارج از دامنه: Payroll کامل)
۷. **TD-HSE-01** — در کامیت مستقل و سراسری

---

## ۱۰. سلامت مخزن

✅ `npm test` ۱۶۸۰/۱۶۸۰ · tsc ۵۷ (baseline ثابت) · build سبز
✅ صفر `DROP` و صفر `ALTER` مخرب در هر ۱۷ مهاجرت
✅ ظاهر، فونت Vazirmatn، `src/index.css` و `vite.config.ts` دست‌نخورده
✅ نام ۶ زیرماژول FIN و ساختار منو تغییر نکرده
⚠️ ۶ کامیت محلی **push نشده‌اند** — این نشست بسته است؛ انتقال به گیت‌هاب نیازمند نشست جدید است
