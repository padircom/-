/**
 * مسیرهای سند DPR — استاب بازیابی
 * ---------------------------------------------------------------------------
 * چرا این فایل وجود دارد:
 *   `server/index.js` این ماژول را import می‌کند و `registerDprDocRoutes(app)`
 *   را صدا می‌زند، ولی خودِ فایل در ادغام نهایی روی `main` مفقود بود؛ نتیجه
 *   ERR_MODULE_NOT_FOUND در بوت سرور و نایستادن کامل API بود.
 *
 * وضعیت فعلی:
 *   هیچ مسیری از این ماژول در رابط کاربری، تست‌ها یا اسناد ارجاع نمی‌شود
 *   (مسیرهای واقعی DPR — `…/dpr`، `…/dpr/:id`، `…/dpr/:id/actions`،
 *   `…/dpr/conflicts` — همه در `server/index.js` ثبت شده‌اند). پس این استاب
 *   هیچ رفتاری را حذف نمی‌کند؛ فقط نقطهٔ اتصال را زنده نگه می‌دارد.
 *
 * اگر بعداً خروجی سند DPR (Word/PDF) لازم شد، همین‌جا اضافه می‌شود.
 */

/** بدون ثبت مسیر — فقط برای پایدار ماندن بوت سرور. */
export function registerDprDocRoutes(_app) {
  /* عمداً خالی */
}

export default registerDprDocRoutes;
